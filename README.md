# React-Express-Demo-app

## Installation
 - goto main folder and hit `npm install`
 - goto server folder and hit `npm install`

 ## Run app
 - goto main folder and hit `npm start`
 - goto server folder and hit `npm run dev`


